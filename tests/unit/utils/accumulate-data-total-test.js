import accumulateDataTotal from 'asreport/utils/accumulate-data-total';
import { module, test } from 'qunit';

module('Unit | Utility | accumulate data total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = accumulateDataTotal();
  assert.ok(result);
});

  Show&nbsp;
  {{simple-select
      options=labels
      choice=label
      onchange=(action updateChart)
  }}
  &nbsp;data

{{ember-chart
    type='line'
    data=chartData
    options=chartOptions
    animate=true
}}